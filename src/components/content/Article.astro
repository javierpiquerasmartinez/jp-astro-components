---
import type { ArticleProps } from './Article.types';

type Props = ArticleProps;

const { label, class: className, ...rest } = Astro.props;
---

<article
  class:list={['jp-article', className]}
  aria-label={label}
  {...rest}
>
  <slot />
</article>
